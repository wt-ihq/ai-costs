# Dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# Next.js
/.next/
/out/
/build
next-env.d.ts

# Production
/dist

# Environment / secrets (vendor API keys live in Vercel env vars only)
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# Supabase
/supabase/.branches
/supabase/.temp

# Testing
/coverage

# TypeScript
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local source samples — real employee names/emails/spend (HR-adjacent PII).
# Kept out of git; tests use inline fixtures, not these files.
/reports/

# Editor / OS
.DS_Store
*.pem
.idea
.vscode/*
!.vscode/extensions.json

# Local credit-usage CSV exports (contain employee PII — never commit)
analytics/
